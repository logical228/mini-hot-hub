# 生产环境后端 API 基地址（构建时注入，见 src/api/client.ts）
# 示例：https://your-backend.railway.app/api
# 留空或未设置时，默认使用相对路径 /api（需同域反代或 CDN 配置）
# VITE_API_BASE=

# 页脚「更新频率」展示用，单位：秒；应与后端 CACHE_TTL 保持一致（默认 600 = 10 分钟）
# VITE_CACHE_TTL=600
